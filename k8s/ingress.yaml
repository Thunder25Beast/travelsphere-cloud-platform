apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: travelsphere-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: travelsphere-service
                port:
                  number: 80
